# Production site URL (no trailing slash). Used for canonical URLs, Open Graph and sitemap.
# On Vercel: set this in Project → Settings → Environment Variables (Production).
# If unset, build falls back to VERCEL_PROJECT_PRODUCTION_URL or VERCEL_URL.
VITE_SITE_URL=https://peterdinis.dev
